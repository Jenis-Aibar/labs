CREATE DATABASE phonebook;
\c phonebook;

1.
-------------------------------------------
CREATE TABLE mainData (
    id BIGSERIAL NOT NULL PRIMARY KEY,
    firstname VARCHAR (20) NOT NULL,
    lastname VARCHAR (25),
    contact VARCHAR (15) UNIQUE NOT NULL
);

\d mainData;
-------------------------------------------
2.
-------------------------------------------
a)

\copy mainData (firstname, lastname, contact)
FROM 'labs\lab10\data.csv'
DELIMITER ','
CSV HEADER;

b)

INSERT INTO mainData (firstname, lastname, contact) VALUES
('Диас', '', '87000561245'),
('Сакен', '', '87477893321'),
('Айбар', '', '87781584338');
-------------------------------------------
3.
-------------------------------------------
UPDATE mainData
SET contact = '87781584337'
WHERE firstname = 'Айбар';
-------------------------------------------
4.
-------------------------------------------
//All

SELECT * FROM mainData;

// firstame and contact from name
	
SELECT firstname, contact FROM mainData WHERE firstname LIKE 'A%' or firstname LIKE 'a%';

// Firstname and contact from contact

SELECT firstname, contact FROM mainData WHERE contact LIKE '_747%';     // Activ)

// Interval [a, b] // a, b is integer

SELECT * FROM mainData WHERE id >= a AND id <= b;
-------------------------------------------
5.
-------------------------------------------
DELETE FROM mainData
WHERE contact = '87781584337';
-------------------------------------------